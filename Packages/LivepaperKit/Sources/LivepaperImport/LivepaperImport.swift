/// Turns files the user provides into library wallpapers: discover, fingerprint,
/// probe, plan, convert, normalise, generate artefacts, validate the loop seam, commit.
///
/// Empty until milestone M4 (docs/roadmap.md).
public enum LivepaperImport {}
